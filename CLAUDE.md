# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The project already documents itself in `AGENTS.md` (read by Codex/Cursor too). That file is the single source of truth — it is imported below, so edit `AGENTS.md`, not this file.

@AGENTS.md
